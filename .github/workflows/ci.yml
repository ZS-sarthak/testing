name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'development'
  pull_request:
    types: [ opened, edited, reopened ]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - id: step1
        run: echo "test=hello"
